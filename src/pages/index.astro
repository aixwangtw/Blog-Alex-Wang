---
// ============================================================
// 首頁（2026-08 改版第三階段）
// ------------------------------------------------------------
// 版面、字級、字距補償、全形問號修剪、捲動揭露的唯一真實來源是
// public/_preview/home-b4-lean.html（變體 B-v4「精簡版」）。
// 這個檔案是把該原型做進 Astro，頁首與頁尾改由 src/layouts/Base.astro 提供。
//
// 改版前是 239 行、七個區塊（免費資源、教學案例、演講經歷、教學內容、
// 精選文章、FAQ、CTA）。原型的結論是：首頁字放到 96px 也救不了資訊過載，
// 所以砍到五個單元 —— 頁首／Hero（含三張服務卡）／信任證明／結尾 CTA／頁尾。
//
// 被移除的區塊都另有去處，沒有內容因此消失：
//   TEACHING_CASES      → /services/one-on-one/、/about/
//   SPEAKING_ENGAGEMENTS→ /services/speaking/、/about/
//   TEACHING_CONTENT    → /services/one-on-one/
//   HOME_FAQS           → /faq/
//   精選文章             → /blog/
//   VIDEOS              → /resources/（本階段一併新建；VIDEOS 原本只有首頁在用）
//
// schema 的兩個處理，方向相反，不可弄錯：
//   Course  保留。它的 teaches 來自 SYLLABUS，而 SYLLABUS 本來就不 render 成畫面、
//           只餵 schema，因此區塊移除不影響它，資料與 schema 都留著。
//   FAQPage 移除。/faq/ 已經有 2 份 FAQPage（HOME_FAQS ＋ 各文章 FAQ），
//           首頁那份從一開始就是重複的。
// ============================================================
import { Image } from 'astro:assets';
import { getCollection } from 'astro:content';
import freeResourcesHome from '../assets/free-resources-home-v2.png';
import instructorShirt from '../assets/instructor-shirt.jpg';
import taichungTalk from '../assets/taichung-talk.jpg';
import { AUTHOR, LINKS, SITE, pageBannerUrl } from '../consts';
import { SYLLABUS } from '../data/home';
import { TEACHING_CASES } from '../data/services';
import Base from '../layouts/Base.astro';

const featuredPosts = (await getCollection('blog', ({ data }) => !data.unlisted && data.featured))
  .sort(
    (a, b) =>
      b.data.pubDate.valueOf() - a.data.pubDate.valueOf() ||
      (b.data.cmsId ?? 0) - (a.data.cmsId ?? 0),
  )
  .slice(0, 3);

const schema = [
  {
    '@type': 'Course',
    '@id': `${SITE.url}/#course`,
    name: 'AI 新手實戰課：桌面版 Agent 與社群自動發文',
    description:
      '手把手帶零基礎學員從 0 到 1 完成第一個作品，包含桌面版 AI Agent 安裝、實作、測試與除錯。提供雙北實體與線上私人家教。',
    provider: { '@id': AUTHOR.id },
    inLanguage: 'zh-TW',
    courseMode: ['onsite', 'online'],
    teaches: SYLLABUS.map((s) => s.title),
  },
];
---

<Base
  title="AI新手教學｜零基礎學 Claude Code / Codex 與社群自動發文"
  description={SITE.description}
  ogImage={pageBannerUrl('home')}
  ogImageAlt="AI 新手教學｜從工具到自動化"
  schema={schema}
>
  <div class="home" id="home-root">
    <div class="home-shell">
      <!-- 1／Hero：手機首屏放核心主張、LINE 行動入口與三項可完成成果 -->
      <section class="hero" aria-labelledby="hero-h">
        <div class="hero-top">
          <h1 class="q q--display hero-reveal" id="hero-h" style="--d:0ms">手把手教學<br />帶你從 <span class="lat">0</span> 到 <span class="lat">1</span><br />完成第一個作品</h1>
          <div class="reply">
            <div class="hero-reveal" style="--d:70ms">
              <p class="by"><span class="by-name micro"><span class="lat">ALEX</span> 私人家教</span></p>
              <p class="lede">不需要技術背景，第一次帶你把工作流跑通，後續只靠對話就能使用。</p>
            </div>
            <div class="terminal-demo hero-reveal" aria-label="從安裝、操作、排錯到上線，一條龍帶你完成" style="--d:110ms">
              <div class="terminal-bar" aria-hidden="true">
                <span></span><span></span><span></span><b><span class="lat">AI Agent</span></b>
              </div>
              <div class="terminal-screen">
                <p class="terminal-command"><span class="terminal-prompt">❯</span><span class="terminal-typed">從安裝 &gt; 操作 &gt; 排錯 &gt; 上線</span><i aria-hidden="true"></i></p>
                <div class="terminal-state" aria-hidden="true">
                  <span class="terminal-thinking"><i></i>全部打包成自動化流程…</span>
                  <strong class="terminal-done"><span aria-hidden="true">✓</span> 一條龍帶你完成</strong>
                </div>
              </div>
            </div>
            <div class="actions hero-reveal" style="--d:140ms">
              <a class="btn btn-line" href={LINKS.line} target="_blank" rel="noreferrer">
                <span class="line-ic" aria-hidden="true"><svg viewBox="0 0 24 24" focusable="false"><path d="M19.365 9.863c.349 0 .63.285.63.631 0 .345-.281.63-.63.63H17.61v1.125h1.755c.349 0 .63.283.63.63 0 .344-.281.629-.63.629h-2.386c-.345 0-.627-.285-.627-.629V8.108c0-.345.282-.63.63-.63h2.386c.346 0 .627.285.627.63 0 .349-.281.63-.63.63H17.61v1.125h1.755zm-3.855 3.016c0 .27-.174.51-.432.596-.064.021-.133.031-.199.031-.211 0-.391-.09-.51-.25l-2.443-3.317v2.94c0 .344-.279.629-.631.629-.346 0-.626-.285-.626-.629V8.108c0-.27.173-.51.43-.595.06-.023.136-.033.194-.033.195 0 .375.104.495.254l2.462 3.33V8.108c0-.345.282-.63.63-.63.345 0 .63.285.63.63v4.771zm-5.741 0c0 .344-.282.629-.631.629-.345 0-.627-.285-.627-.629V8.108c0-.345.282-.63.63-.63.346 0 .628.285.628.63v4.771zm-2.466.629H4.917c-.345 0-.63-.285-.63-.629V8.108c0-.345.285-.63.63-.63.348 0 .63.285.63.63v4.141h1.756c.348 0 .629.283.629.63 0 .344-.282.629-.629.629M24 10.314C24 4.943 18.615.572 12 .572S0 4.943 0 10.314c0 4.811 4.27 8.842 10.035 9.608.391.082.923.258 1.058.59.12.301.079.766.038 1.08l-.164 1.02c-.045.301-.24 1.186 1.049.645 1.291-.539 6.916-4.078 9.436-6.975C23.176 14.393 24 12.458 24 10.314" /></svg></span>
                <span class="btn-l">加 <span class="lat">LINE</span> 免費諮詢</span>
              </a>
            </div>
          </div>
        </div>

        <section class="outcomes-block hero-reveal" aria-labelledby="outcomes-h" style="--d:190ms">
          <h2 class="outcomes-h" id="outcomes-h">你可以完成</h2>
          <ul class="outcomes-list">
            <li>
              <span class="outcome-icon" aria-hidden="true">
                <svg viewBox="0 0 32 32" focusable="false"><path d="m11 8-6 8 6 8M21 8l6 8-6 8M18.5 6l-5 20" /></svg>
              </span>
              <span class="outcome-copy">
                <strong><span class="lat">Vibe Coding</span></strong>
                <span>帶你用提示詞解耦，不再改 <span class="lat">A</span> 壞 <span class="lat">B</span></span>
              </span>
            </li>
            <li>
              <span class="outcome-icon" aria-hidden="true">
                <svg viewBox="0 0 32 32" focusable="false"><rect x="4" y="7" width="24" height="18" rx="2" /><path d="m14 12 7 4-7 4z" /></svg>
              </span>
              <span class="outcome-copy">
                <strong><span class="lat">AI</span> 拍片</strong>
                <span>從腳本、配音到剪輯與發佈</span>
              </span>
            </li>
            <li>
              <span class="outcome-icon" aria-hidden="true">
                <svg viewBox="0 0 32 32" focusable="false"><circle cx="7" cy="16" r="3" /><circle cx="25" cy="8" r="3" /><circle cx="25" cy="24" r="3" /><path d="M10 16h6m0 0 6-6m-6 6 6 6" /></svg>
              </span>
              <span class="outcome-copy">
                <strong>社群自動化</strong>
                <span>串接帳號，完成自動發文與排程</span>
              </span>
            </li>
          </ul>
        </section>
      </section>

      <!-- 2／信任證明：服務入口已由下方四個詳細區塊負責，不重複放卡片 -->
      <section class="hero-followup" aria-label="講師資歷">
        <p class="trust hero-reveal" style="--d:280ms"><b>接案工程師、資策會講師</b></p>
      </section>

      <!-- 3／服務詳細介紹：服務入口的下一層資訊 -->
      <section class="service-details" aria-label="服務詳細介紹">
        <article class="service-row reveal">
          <div class="service-copy">
            <p class="service-kicker">先自己試試看</p>
            <h2>免費資源</h2>
            <p>先用免費教學影片和開源工具動手做。從 Agent 安裝、Meta API 申請到自動發文，都有可以直接跟著操作的內容。</p>
            <ul>
              <li>免費實作影片</li>
              <li>開源社群圖文工具 Tooka</li>
              <li>卡關再來問，不必急著付費</li>
            </ul>
            <a class="service-link" href="/resources/">查看免費資源 <span aria-hidden="true">→</span></a>
          </div>
          <figure class="service-media">
            <Image src={freeResourcesHome} alt="教學影片、開源工具與實作指南組成的免費 AI 學習資源" widths={[560, 880]} sizes="(min-width: 768px) 45vw, 100vw" />
          </figure>
        </article>

        <article class="service-row service-row--reverse reveal">
          <div class="service-copy">
            <p class="service-kicker">實際學員與專案</p>
            <h2>私人家教案例</h2>
            <p>每堂課都用學員自己的電腦、帳號和需求實作。過去已帶學員完成 Agent 安裝、社群自動發文、Email 與行事曆整理，以及 OpenClaw 排錯救援。</p>
            <ul>
              <li>WordPress 串接 Threads 自動發文</li>
              <li>Codex 搜尋 Threads 品牌輿情</li>
              <li>Claude Code 整理 Email、行事曆與 LINE 提醒</li>
            </ul>
            <a class="service-link" href="/services/one-on-one/">查看完整案例與上課方式 <span aria-hidden="true">→</span></a>
          </div>
          <ul class="case-mini-grid" aria-label="四個私人家教實際案例">
            {TEACHING_CASES.map((item) => (
              <li>
                <Image src={item.image} alt={item.imageAlt} width={64} class="case-mini-icon" />
                <div>
                  <strong>{item.topic}</strong>
                  <span>{item.student}</span>
                </div>
              </li>
            ))}
          </ul>
        </article>

        <article class="service-row reveal">
          <div class="service-copy">
            <p class="service-kicker">現場演示與實作</p>
            <h2>演講紀錄</h2>
            <p>曾於企業、AI 協會與地方社群分享 AI Agent、社群圖文與對話式拍片。內容不只講概念，會把真實工作流搬到現場演示。</p>
            <ul>
              <li>MixerBox：OpenClaw 雙機斷線救援</li>
              <li>TAIA：只靠對話一條龍完成拍片</li>
              <li>台中、雲林：AI 社群圖文實作</li>
            </ul>
            <a class="service-link" href="/services/speaking/">查看全部演講紀錄與邀約方式 <span aria-hidden="true">→</span></a>
          </div>
          <figure class="service-media">
            <Image src={taichungTalk} alt="Alex 在台中場進行 AI 社群圖文演講" widths={[560, 880]} sizes="(min-width: 768px) 45vw, 100vw" />
          </figure>
        </article>

        <section class="instructor-profile reveal" aria-labelledby="instructor-h">
          <figure class="instructor-photo">
            <Image src={instructorShirt} alt="AI 應用講師 Alex" widths={[420, 700]} sizes="(min-width: 768px) 34vw, 100vw" />
          </figure>
          <div class="instructor-main">
            <p class="service-kicker">從非科班到 AI 應用講師</p>
            <h2 id="instructor-h">關於講師</h2>
            <p class="instructor-name"><span class="lat">Alex Wang</span>／王士麟</p>
            <p class="instructor-intro">接案工程師與資策會講師，也是從沒有技術背景開始，透過 AI 一步一步學會寫程式、做自動化與完成客戶專案，因此特別知道新手容易卡在哪裡。</p>
            <div class="instructor-actions">
              <a class="service-link" href="/about/">了解講師經歷 <span aria-hidden="true">→</span></a>
              <nav class="instructor-socials" aria-label="講師社群連結">
                <a href={LINKS.threads} target="_blank" rel="noreferrer" aria-label="Alex 的 Threads（開新分頁）">
                  <svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M12.186 24h-.007c-3.581-.024-6.334-1.205-8.184-3.509C2.35 18.44 1.5 15.586 1.472 12.01v-.017c.03-3.579.879-6.43 2.525-8.482C5.845 1.205 8.6.024 12.18 0h.014c2.746.02 5.043.725 6.826 2.098 1.677 1.29 2.858 3.13 3.509 5.467l-2.04.569c-1.104-3.96-3.898-5.984-8.304-6.015-2.91.022-5.11.936-6.54 2.717C4.307 6.504 3.616 8.914 3.589 12c.027 3.086.718 5.496 2.057 7.164 1.43 1.783 3.631 2.698 6.54 2.717 2.623-.02 4.358-.631 5.8-2.045 1.647-1.613 1.618-3.593 1.09-4.798-.31-.71-.873-1.3-1.634-1.75-.192 1.352-.622 2.446-1.284 3.272-.886 1.102-2.14 1.704-3.73 1.79-1.202.065-2.361-.218-3.259-.801-1.063-.689-1.685-1.74-1.752-2.964-.065-1.19.408-2.285 1.33-3.082.88-.76 2.119-1.207 3.583-1.291a13.853 13.853 0 0 1 3.02.142c-.126-.742-.375-1.332-.75-1.757-.513-.586-1.308-.883-2.359-.89h-.029c-.844 0-1.992.232-2.721 1.32L7.734 7.847c.98-1.454 2.568-2.256 4.478-2.256h.044c3.194.02 5.097 1.975 5.287 5.388.108.046.216.094.321.142 1.49.7 2.58 1.761 3.154 3.07.797 1.82.871 4.79-1.548 7.158-1.85 1.81-4.094 2.628-7.277 2.65Zm1.003-11.69c-.242 0-.487.007-.739.021-1.836.103-2.98.946-2.916 2.143.067 1.256 1.452 1.839 2.784 1.767 1.224-.065 2.818-.543 3.086-3.71a10.5 10.5 0 0 0-2.215-.221Z" /></svg>
                </a>
                <a href={LINKS.youtube} target="_blank" rel="noreferrer" aria-label="Alex 的 YouTube（開新分頁）">
                  <svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M23.498 6.186a3.003 3.003 0 0 0-2.112-2.123C19.505 3.5 12 3.5 12 3.5s-7.505 0-9.386.563A3.003 3.003 0 0 0 .502 6.186 31.3 31.3 0 0 0 0 12a31.3 31.3 0 0 0 .502 5.814 3.003 3.003 0 0 0 2.112 2.123C4.495 20.5 12 20.5 12 20.5s7.505 0 9.386-.563a3.003 3.003 0 0 0 2.112-2.123A31.3 31.3 0 0 0 24 12a31.3 31.3 0 0 0-.502-5.814ZM9.545 15.568V8.432L15.818 12l-6.273 3.568Z" /></svg>
                </a>
              </nav>
            </div>
          </div>
          <dl class="instructor-facts">
            <div><dt>角色</dt><dd>AI 應用講師<br />全職接案工程師</dd></div>
            <div><dt>專長</dt><dd>AI Agent<br />Vibe Coding<br />工作流自動化</dd></div>
            <div><dt>教學</dt><dd>雙北實體<br />線上私人家教<br />企業與社群演講</dd></div>
          </dl>
        </section>

        {featuredPosts.length > 0 && (
          <section class="featured-posts reveal" aria-labelledby="featured-h">
            <div class="featured-head">
              <div>
                <p class="service-kicker">LATEST ARTICLES</p>
                <h2 id="featured-h">精選文章</h2>
              </div>
              <a href="/blog/">查看全部文章 <span aria-hidden="true">→</span></a>
            </div>
            <ul class="featured-list">
              {featuredPosts.map((post) => (
                <li>
                  <a href={`/blog/${post.id}/`}>
                    <img src={`/images/blog/${post.id}-thumb.webp`} alt="" width="640" height="336" loading="lazy" />
                    <p class="featured-meta"><span>{post.data.tags[0] ?? 'AI 教學'}</span><time datetime={post.data.pubDate.toISOString()}>{post.data.pubDate.toLocaleDateString('zh-TW')}</time></p>
                    <h3>{post.data.title}</h3>
                    <span class="featured-go" aria-hidden="true">閱讀全文 →</span>
                  </a>
                </li>
              ))}
            </ul>
          </section>
        )}
      </section>

      <!-- 4／結尾 CTA -->
      <section class="closer" aria-labelledby="closer-h">
        <h2 class="q q--sec reveal" id="closer-h"><span class="nb">不確定自己適合哪一種<span class="qt">？</span></span></h2>
        <div class="reveal" style="--d:70ms">
          <p class="closer-p">加 <span class="lat">LINE</span> 直接說你想自動化什麼。我會告訴你該走哪一條，也可能直接告訴你哪篇免費文章就能解決。</p>
          <div class="actions">
            <a class="btn btn-line" href={LINKS.line} target="_blank" rel="noreferrer">
              <span class="line-ic" aria-hidden="true"><svg viewBox="0 0 24 24" focusable="false"><path d="M19.365 9.863c.349 0 .63.285.63.631 0 .345-.281.63-.63.63H17.61v1.125h1.755c.349 0 .63.283.63.63 0 .344-.281.629-.63.629h-2.386c-.345 0-.627-.285-.627-.629V8.108c0-.345.282-.63.63-.63h2.386c.346 0 .627.285.627.63 0 .349-.281.63-.63.63H17.61v1.125h1.755zm-3.855 3.016c0 .27-.174.51-.432.596-.064.021-.133.031-.199.031-.211 0-.391-.09-.51-.25l-2.443-3.317v2.94c0 .344-.279.629-.631.629-.346 0-.626-.285-.626-.629V8.108c0-.27.173-.51.43-.595.06-.023.136-.033.194-.033.195 0 .375.104.495.254l2.462 3.33V8.108c0-.345.282-.63.63-.63.345 0 .63.285.63.63v4.771zm-5.741 0c0 .344-.282.629-.631.629-.345 0-.627-.285-.627-.629V8.108c0-.345.282-.63.63-.63.346 0 .628.285.628.63v4.771zm-2.466.629H4.917c-.345 0-.63-.285-.63-.629V8.108c0-.345.285-.63.63-.63.348 0 .63.285.63.63v4.141h1.756c.348 0 .629.283.629.63 0 .344-.282.629-.629.629M24 10.314C24 4.943 18.615.572 12 .572S0 4.943 0 10.314c0 4.811 4.27 8.842 10.035 9.608.391.082.923.258 1.058.59.12.301.079.766.038 1.08l-.164 1.02c-.045.301-.24 1.186 1.049.645 1.291-.539 6.916-4.078 9.436-6.975C23.176 14.393 24 12.458 24 10.314" /></svg></span>
              <span class="btn-l">加 <span class="lat">LINE</span> 免費諮詢</span>
            </a>
          </div>
        </div>
      </section>
    </div>
  </div>

  <script is:inline>
    /* 逐段浮現：內容 100% 寫在初始 HTML 裡，只有「顯示與否」交給 CSS。
       `js` 這個 class 加在 #home-root（不是 <html>）上，因為這一頁的樣式是
       Astro 的元件範圍樣式——選擇器會被加上 data-astro-cid-*，
       掛在 <html> 上的 class 選不到。
       三層保險，任何一層失效內容都仍然完整可見：
         1. 沒有 JS（或這段拋例外）→ `js` 不會加上 → 所有 .reveal 維持原樣可見
         2. IntersectionObserver 不存在／被停用／行為異常 → 2 秒逾時強制顯示
         3. prefers-reduced-motion → 直接全部顯示，且 CSS 也把過渡關掉 */
    (function () {
      var root = document.getElementById('home-root');
      if (!root) return;
      var reveals = root.querySelectorAll('.reveal, .hero-reveal');

      function revealAll() {
        for (var i = 0; i < reveals.length; i++) reveals[i].classList.add('is-in');
      }

      try {
        root.classList.add('js');

        // 保險 1：先掛逾時，再做任何可能爆炸的事
        var ioAlive = false;
        setTimeout(function () {
          if (!ioAlive) revealAll();
        }, 2000);

        var reduce =
          window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches;

        // 首屏：立刻揭露（0 / 70 / 140ms 延遲，各 200ms → 總揭露 340ms）
        var hero = root.querySelectorAll('.hero-reveal');
        requestAnimationFrame(function () {
          requestAnimationFrame(function () {
            for (var i = 0; i < hero.length; i++) hero[i].classList.add('is-in');
          });
        });

        if (reduce || !('IntersectionObserver' in window)) {
          revealAll();
          return;
        }

        var io = new IntersectionObserver(
          function (entries) {
            ioAlive = true; // 觀察器確實會回呼 → 逾時保險不必動作
            entries.forEach(function (e) {
              if (e.isIntersecting) {
                e.target.classList.add('is-in');
                io.unobserve(e.target);
              }
            });
          },
          { rootMargin: '0px 0px -8% 0px', threshold: 0.12 },
        );

        var scrollReveals = root.querySelectorAll('.reveal');
        for (var k = 0; k < scrollReveals.length; k++) io.observe(scrollReveals[k]);
      } catch (err) {
        revealAll(); // 保險 2
      }
    })();
  </script>

  <style>
    /* ============================================================
       首頁專屬排版。這裡的每一條都只作用在首頁，不外洩到內頁：
       - 展示級 H1（96px）與襯線體是首頁專用（設計語言定案 #1）
       - 字距分級只套在拉丁字段（.lat）上，中文一律 0
       - 顏色一律用 global.css @theme 的 --color-*，這裡不重新定義任何顏色
       - 內頁字級 .t-h1／.t-h2／.t-h3 這裡完全不碰
       ============================================================ */
    .home {
      /* ---- 字距分級（依光學尺寸，不是一個值套到底）----
         只套在拉丁字段（.lat）上；中文一律 0。
         --ls-display 供 ≥72px 的字段使用。H1 是 clamp(56,8vw,96)，
         8vw 在 viewport 900px 時剛好等於 72px，因此 display 級只在
         min-width:900px 之後才接上；900px 以下 H1 是 56–71.9px，留在 title 級。 */
      --ls-display: -0.06em;
      --ls-title: -0.028em;
      --ls-body: -0.011em;
      --ls-micro: 0.12em;
      --ls: var(--ls-body); /* 拉丁字段預設用內文級 */

      color: var(--color-ink-2);
      font-size: 16px;
      line-height: 1.9;
      letter-spacing: 0; /* 中文：0，不商量 */
    }

    /* 版心對齊 Base 的頁首頁尾（.hd-cap／.ft-wrap 都是 max-width:72rem、
       padding 20/40px）。原型的 .shell 是 1120px，這裡改用 72rem＝1152px，
       為的是 H1 的左緣跟頁首字標、頁尾品牌塊落在同一條垂直線上——
       兩者差 32px 時，捲動經過頁首那條線會看得出來。 */
    .home-shell {
      width: 100%;
      max-width: 72rem;
      margin: 0 auto;
      padding: 0 20px;
    }
    @media (min-width: 768px) {
      .home-shell {
        padding: 0 40px;
      }
    }

    /* ---- 拉丁字段：字距只加在這裡，並補回「尾字距」----
       CSS 的 letter-spacing 會在字段最後一個字元「後面」也加一份間距，
       負值會把後面的中文吸過來、正值會推開。用等量反向的 margin 補回。
       原型是用 .lat 包裹、由 Python 腳本在建置期產生；Astro 沒有那一步，
       首頁內容已砍到 470 字上下，.lat 的位置不多，直接手寫在模板裡。 */
    .lat {
      font-style: normal;
      letter-spacing: var(--ls, 0em);
      margin-inline-end: calc(-1 * var(--ls, 0em));
    }

    /* 12–13px 小標籤：唯一反向加字距的地方（正值，中文也適用），
       同樣要補回尾字距，否則標籤右側會多出一份空白。 */
    .micro {
      letter-spacing: var(--ls-micro);
      margin-inline-end: calc(-1 * var(--ls-micro));
      --ls: 0em; /* 內層 .lat 不再疊加 */
    }

    /* ============================================================
       全形問號的左空白修剪
       ------------------------------------------------------------
       問題：96px 下句末的全形「？」左邊留一個 27px 的空洞。
       實測（Noto Serif TC，upm 1000）：
         U+FF1F  advance 1.000em，墨跡 0.254em → 0.730em
                 左空白 0.254em、右空白 0.270em
         相鄰漢字 始 rsb 0.043em／大 rsb 0.049em／哪 lsb 0.068em
       → 漢字對漢字的墨跡間距約 0.068em，漢字對「？」是 0.297em，差 3.7 倍。
         Chrome 147／96px 實際渲染量測：始？＝28px，始始＝7px。

       ---- 三條「正規解法」全部驗證過，全部不能用 ----
       1. font-feature-settings:"palt" 1
          ／font-variant-east-asian:proportional-width
          Noto Serif TC 的 GPOS 裡「沒有 palt」。108 個子集抽驗 3 個
          （含實際含 U+FF1F 的那個），GPOS 只有 chws/halt/kern/vchw/vhal/vpal。
          halt 的涵蓋範圍只有「」（）！ 等六個字符，chws 只處理成對括號，
          兩者都不含 U+FF1F。實測結果：間距 26px 完全不動。
          同一段 CSS 換成 Noto Sans TC 就有效（27px → 12px）——Sans 有 palt
          且涵蓋 U+FF1F（XPlacement −134／XAdvance −283）。
          也就是說語法沒錯，是這支襯線字型沒有這個功能。
          另外 font-variant-east-asian:proportional-width 對應的是 pwid 而非
          palt，連 Noto Sans TC 都沒有效果（實測 27px 不動）。
       2. text-spacing-trim（CSS Text Level 4）
          Chrome 147 只接受 normal / trim-start / space-all；
          trim-both、trim-all、auto 全部 invalid（CSS.supports 回 false）。
          功能本身確實有在運作：trim-start 會把行首的「切掉左半格
          （墨跡左緣 163px → 115px，正好 0.5em）。
          但三個合法值對「？」的渲染結果一模一樣（26px 不動）——
          規格只裁全形的「開頭／結尾標點」，全形問號、驚嘆號不在裡面。
       3. GSUB 的 hwid／pwid 寬度替代字符
          Noto Serif TC 的 GSUB 只有 ccmp/liga/locl/vert/vrt2，沒有寬度替代。

       ---- 結論與作法 ----
       這支字型沒有任何 OpenType 或 CSS 標準機制可用，只剩光學修剪一條路。
       修剪量不是喬出來的：0.254em（？的左空白）− 0.05em（保留的殘餘空白）
       = 0.204em。留下的 0.05em 讓「？」的墨跡間距落回漢字的節奏上。
       實測驗收（Chrome 147／96px）：始？ 28px → 9px（對照 始始 ＝ 7px）、
                                     大？ 26px → 7px（對照 大大 ＝ 5px）。
       比漢字節奏鬆 2px，這是對的——問號本來就該比字距多留一點點氣。

       副作用與圍堵：
       - 只作用在被 .qt 包起來的那一個字元。頁面其他地方的「，」「。」
         完全不受影響（實測 大，大 加不加 .qt 都是 41/43px）。
       - 修剪量是 em，隨字級等比縮放，所有斷點的光學結果一致，
         不需要為不同字級各補一次。
       - 換行風險：用 .nb（white-space:nowrap）把「開始？」綁成一個
         不可斷的單位，「？」永遠不可能落到行首，也就不會把負 margin
         拉到容器左緣外面。
       - 字型 fallback 風險：若 Noto Serif TC 沒載入，改用 Songti TC 等
         備援字型時修剪量可能不完全匹配。只有在備援字型的「？」左空白
         小於 0.204em 時才會過修——絕大多數中文字型都是 0.24–0.28em，
         風險低，但這條無法在本機驗證，仍屬已知風險。
       ============================================================ */
    .qt {
      margin-inline-start: -0.204em;
    }
    .nb {
      white-space: nowrap;
    }

    /* ---------- Hero ---------- */
    .hero {
      padding-top: 28px;
    }
    /* Hero 到三項「你可以完成」成果為真正的首屏；服務入口放在下一個 section。
       手機使用 svh，避免 Threads／Instagram 等內建瀏覽器把網址列高度
       算進 100vh，造成看似剛好、實機卻被工具列裁掉。min-height 只留白，
       不限制內容高度，所以矮螢幕或放大字級時仍會自然往下增高。 */
    @media (max-width: 767px) {
      .hero {
        min-height: calc(100vh - var(--bar));
        min-height: calc(100svh - var(--bar));
      }
    }
    @media (min-width: 768px) {
      .hero {
        padding-top: 56px;
      }
    }
    @media (min-width: 900px) {
      .hero-top {
        display: grid;
        grid-template-columns: minmax(0, 1.05fr) minmax(360px, 0.95fr);
        gap: clamp(40px, 5vw, 72px);
        align-items: start;
      }
    }
    @media (min-width: 1200px) {
      .hero {
        padding-top: 68px;
      }
    }

    /* 提問＝襯線，回答＝無襯線。這組雙聲音系統是首頁專用的（設計語言定案 #1）。
       原型刻意不把提問放進暖灰面板：面板是「泡泡」的親戚，而且會把標題的
       可用寬度砍掉 64px，正好吃掉氣勢的來源。 */
    .q {
      font-family: var(--font-serif);
      /* 字重寫 600 而不是原型的 700／560：全站只自架 Noto Serif TC 600
         與 Noto Sans TC 400/600（見 global.css 開頭的實測字重清單），
         寫 600 才是精準命中已載入的那個字面，不必經過 CSS 的近似匹配。 */
      font-weight: 600;
      color: var(--color-ink);
      letter-spacing: 0; /* 中文 0 */
      --ls: var(--ls-title); /* 拉丁字段收 -.028em */
      overflow-wrap: break-word;
    }
    /* 首屏大標：line-height 恰為 1 —— 中文的下限。
       Noto Serif TC 墨跡 0.859 + 0.094 = 0.953em，
       line-height 1 時上下行墨跡淨空 = .047em（96px 下 4.51px）。
       絕不可用 .84：那是英文數值，96px 下兩行墨跡會重疊 10.8px。 */
    .q--display {
      font-size: clamp(40px, 5.5vw, 68px);
      line-height: 1;
      padding-bottom: 0.04em; /* 墨跡略低於行框底 .0265em，留一點 */
    }
    /* 大螢幕的 H1 進入 display 字距級。 */
    @media (min-width: 900px) {
      .q--display {
        --ls: var(--ls-display);
      }
    }

    /* 區塊提問：實測淨空 = 1.15 − 0.953 = .197em。
       上限 28px 與內頁的 .t-h2 同一階（global.css 已如此定義），
       但這裡不套 .t-h2——首頁的 h2 是襯線提問、行高 1.15，
       跟內頁那顆無襯線 1.25 的 h2 是兩種聲音。 */
    .q--sec {
      font-size: clamp(20px, 3vw, 28px);
      line-height: 1.15;
    }

    /* 回答：縮排 ＋ 酒紅細軌 ＋ 無襯線 */
    .reply {
      position: relative;
      margin-top: 30px;
      padding-left: 20px;
      border-left: 2px solid var(--color-accent-line);
    }
    .reply::before {
      content: '';
      position: absolute;
      left: -2px;
      top: 0;
      width: 2px;
      height: 34px;
      background: var(--color-accent);
    }
    @media (min-width: 900px) {
      .reply {
        margin: 8px 0 0;
        padding-left: 24px;
      }
    }

    .by {
      margin-bottom: 12px;
    }
    .by-name {
      font-size: 12.5px;
      font-weight: 600;
      color: var(--color-accent);
    }
    .reply p {
      max-width: 60ch;
    }
    .lede {
      font-size: 16.5px;
      line-height: 1.95;
      color: var(--color-ink-2);
    }
    @media (min-width: 768px) {
      .lede {
        font-size: 17.5px;
      }
    }
    .reply strong {
      color: var(--color-ink);
      font-weight: 600;
    }

    /* ---------- 終端機動畫與作品成果卡 ---------- */
    .outcomes-block {
      margin-top: 32px;
    }
    .hero-followup {
      display: flow-root;
    }
    .outcomes-h {
      margin-bottom: 12px;
      font-size: 12px;
      font-weight: 600;
      letter-spacing: 0.12em;
      color: var(--color-accent);
    }
    .terminal-demo {
      width: 100%;
      max-width: 60ch;
      margin-top: 16px;
      border: 1px solid #2d2b2b;
      border-radius: 5px;
      background: #191818;
      color: #f7f5f1;
      overflow: hidden;
    }
    .terminal-bar {
      display: flex;
      align-items: center;
      gap: 5px;
      height: 25px;
      padding: 0 10px;
      border-bottom: 1px solid #343232;
      background: #242222;
    }
    .terminal-bar > span {
      width: 6px;
      height: 6px;
      border-radius: 50%;
      background: #77716e;
    }
    .terminal-bar b {
      margin-inline-start: 4px;
      font-family: var(--font-mono);
      font-size: 9.5px;
      line-height: 1;
      font-weight: 400;
      color: #aaa4a0;
    }
    .terminal-screen {
      padding: 12px 14px 13px;
      font-family: var(--font-mono);
    }
    .terminal-command {
      display: flex;
      align-items: center;
      min-width: 0;
      font-size: 13px;
      line-height: 1.6;
      white-space: nowrap;
    }
    .terminal-prompt {
      flex: none;
      margin-inline-end: 8px;
      color: #e98797;
    }
    .terminal-typed {
      display: inline-block;
      max-width: 0;
      overflow: hidden;
      animation: terminal-type 6.4s steps(13, end) infinite;
    }
    .terminal-command > i {
      display: inline-block;
      width: 1px;
      height: 1.05em;
      margin-inline-start: 2px;
      background: #f7f5f1;
      animation: terminal-cursor 0.65s step-end infinite;
    }
    .terminal-state {
      position: relative;
      height: 22px;
      margin-top: 7px;
      font-size: 11.5px;
      line-height: 22px;
    }
    .terminal-thinking,
    .terminal-done {
      position: absolute;
      inset: 0 auto auto 0;
    }
    .terminal-thinking {
      color: #bdb7b3;
      animation: terminal-thinking 6.4s var(--ease) infinite;
    }
    .terminal-thinking i {
      display: inline-block;
      width: 6px;
      height: 6px;
      margin-inline-end: 7px;
      border-radius: 50%;
      border: 1px solid #aaa4a0;
      border-top-color: #e98797;
      animation: terminal-spin 0.8s linear infinite;
    }
    .reply .terminal-done {
      font-weight: 600;
      color: #8fd5a6;
      animation: terminal-done 6.4s var(--ease) infinite;
    }
    @keyframes terminal-type {
      0% {
        max-width: 0;
      }
      26%,
      92% {
        max-width: 18em;
      }
    }
    @keyframes terminal-thinking {
      0%,
      28% {
        opacity: 0;
        transform: translateY(4px);
      }
      35%,
      58% {
        opacity: 1;
        transform: translateY(0);
      }
      64%,
      100% {
        opacity: 0;
        transform: translateY(-3px);
      }
    }
    @keyframes terminal-done {
      0%,
      60% {
        opacity: 0;
        transform: translateY(4px);
      }
      68%,
      92% {
        opacity: 1;
        transform: translateY(0);
      }
      100% {
        opacity: 0;
      }
    }
    @keyframes terminal-cursor {
      50% {
        opacity: 0;
      }
    }
    @keyframes terminal-spin {
      to {
        transform: rotate(360deg);
      }
    }
    .outcomes-list {
      display: grid;
      gap: 8px;
      padding: 0;
      list-style: none;
    }
    @media (min-width: 768px) {
      .outcomes-list {
        grid-template-columns: repeat(3, minmax(0, 1fr));
      }
    }
    .outcomes-list li {
      position: relative;
      display: grid;
      grid-template-columns: 42px minmax(0, 1fr);
      gap: 13px;
      align-items: center;
      min-width: 0;
      padding: 15px 16px;
      border: 1px solid var(--color-rule-2);
      border-radius: 4px;
      background:
        linear-gradient(135deg, rgb(255 255 255 / 78%), rgb(255 255 255 / 34%));
      box-shadow: 0 8px 26px rgb(61 42 37 / 5%);
      overflow: hidden;
    }
    .outcomes-list li::before {
      content: '';
      position: absolute;
      inset: 0 0 auto;
      height: 3px;
      background: var(--color-accent);
      opacity: 0.78;
    }
    .outcome-icon {
      display: grid;
      place-items: center;
      width: 42px;
      height: 42px;
      border: 1px solid var(--color-accent-line);
      border-radius: 50%;
      background: rgb(255 255 255 / 64%);
      color: var(--color-accent);
    }
    .outcome-icon svg {
      width: 23px;
      height: 23px;
      fill: none;
      stroke: currentColor;
      stroke-width: 1.8;
      stroke-linecap: round;
      stroke-linejoin: round;
    }
    .outcome-copy {
      display: grid;
      min-width: 0;
      gap: 3px;
    }
    .outcome-copy strong {
      font-size: 16px;
      line-height: 1.4;
      font-weight: 600;
      color: var(--color-ink);
    }
    .outcome-copy > span {
      font-size: 13px;
      line-height: 1.65;
      color: var(--color-ink-3);
    }
    @media (max-width: 767px) {
      .hero {
        padding-top: 14px;
      }
      .q--display {
        font-size: 34px;
      }
      .reply {
        margin-top: 10px;
        padding-left: 14px;
      }
      .reply::before {
        height: 26px;
      }
      .by {
        margin-bottom: 4px;
      }
      .lede {
        font-size: 13.5px;
        line-height: 1.55;
      }
      .terminal-demo {
        margin-top: 10px;
      }
      .terminal-bar {
        height: 20px;
      }
      .terminal-screen {
        padding: 7px 10px 8px;
      }
      .terminal-command {
        font-size: 11.5px;
      }
      .terminal-state {
        height: 16px;
        margin-top: 2px;
        font-size: 10.5px;
        line-height: 16px;
      }
      .reply .actions {
        margin-top: 10px;
      }
      .outcomes-block {
        margin-top: 6px;
      }
      .outcomes-h {
        margin-bottom: 6px;
        line-height: 1.4;
      }
      .outcomes-list {
        gap: 5px;
      }
      .outcomes-list li {
        grid-template-columns: 30px minmax(0, 1fr);
        gap: 9px;
        min-height: 40px;
        padding: 6px 10px;
      }
      .outcomes-list li::before {
        inset: 0 auto 0 0;
        width: 3px;
        height: auto;
      }
      .outcome-icon {
        width: 28px;
        height: 28px;
      }
      .outcome-icon svg {
        width: 17px;
        height: 17px;
      }
      .outcome-copy {
        grid-template-columns: minmax(94px, auto) 1fr;
        align-items: center;
        gap: 8px;
      }
      .outcome-copy strong {
        font-size: 13px;
      }
      .outcome-copy > span {
        font-size: 11px;
        line-height: 1.45;
      }
    }
    /* 320×480 這類極矮舊機：終端機只是流程示意，優先讓核心文案、CTA
       與三項成果完整留在首屏。直接不渲染示意區，不用裁切或負位移硬塞。 */
    @media (max-width: 359px) and (max-height: 540px) {
      .q--display {
        font-size: 31px;
      }
      .terminal-demo {
        display: none;
      }
      .reply .actions {
        margin-top: 6px;
      }
      .outcomes-block {
        margin-top: 4px;
      }
    }
    /* ---------- 按鈕 ---------- */
    .actions {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
      margin-top: 26px;
    }
    .btn {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      font-size: 14.5px;
      font-weight: 500;
      text-decoration: none;
      padding: 12px 22px;
      border-radius: 3px;
      line-height: 1.4;
      /* 44px 觸控目標：12+12 內距 ＋ 14.5×1.4 行框 ≈ 44.3px */
      min-height: 44px;
      transition:
        background-color 0.18s var(--ease),
        border-color 0.18s var(--ease),
        color 0.18s var(--ease),
        transform 0.18s var(--ease);
    }
    .btn-line {
      background: #06c755;
      color: #fff;
      border: 1px solid #06c755;
    }
    .line-ic,
    .line-ic svg {
      display: block;
      width: 20px;
      height: 20px;
    }
    .line-ic svg {
      fill: currentColor;
    }
    /* 按鈕文字必須是「一個」flex 子項。若讓文字節點直接當子項，
       包裹拉丁字段用的 .lat 會被 blockify 成 flex item，gap:8px 就會
       把「加 LINE 免費諮詢」拆成三塊。 */
    .btn-l {
      display: inline;
    }
    .btn .arw {
      font-family: var(--font-mono);
      font-size: 12.5px;
      opacity: 0.85;
      --ls: 0em;
    }

    /* ---------- 信任證明：一行，不做成卡片 ---------- */
    /* 規格是「一行」，所以桌機不設 max-width——這是一行資歷，不是要讀的內文，
       量到 74ch（約 496px）會讓它斷成兩行。窄螢幕本來就放不下，讓它自然換行。 */
    .trust {
      margin-top: 14px;
      font-size: 13.5px;
      line-height: 1.95;
      color: var(--color-ink-3);
    }
    @media (max-width: 767px) {
      .trust {
        margin-top: 10px;
        font-size: 12px;
      }
    }
    .trust b {
      color: var(--color-ink-2);
      font-weight: 500;
    }
    /* 分隔點用 rule-3 而不是 rule-2：它是裝飾字元，但仍要看得見。 */
    .trust .sep {
      color: var(--color-rule-3);
      padding: 0 2px;
    }

    /* ---------- 首屏下方的服務詳細介紹 ---------- */
    .service-details {
      padding-top: clamp(72px, 10vw, 128px);
    }
    .service-row {
      display: grid;
      gap: 30px;
      padding: clamp(48px, 7vw, 88px) 0;
      border-top: 1px solid var(--color-rule);
    }
    @media (min-width: 768px) {
      .service-row {
        grid-template-columns: minmax(0, 1fr) minmax(280px, 0.88fr);
        gap: clamp(48px, 7vw, 96px);
        align-items: center;
      }
      .service-row--reverse .service-copy {
        order: 2;
      }
      .service-row--reverse .service-media {
        order: 1;
      }
    }
    .service-copy {
      max-width: 58ch;
    }
    .service-kicker {
      margin-bottom: 12px;
      font-size: 12px;
      font-weight: 600;
      letter-spacing: 0.1em;
      color: var(--color-accent);
    }
    .service-copy h2 {
      font-family: var(--font-serif);
      font-size: clamp(32px, 4vw, 52px);
      line-height: 1.1;
      font-weight: 600;
      color: var(--color-ink);
    }
    .service-copy > p:not(.service-kicker) {
      margin-top: 20px;
      font-size: 16px;
      line-height: 1.9;
      color: var(--color-ink-2);
    }
    .service-copy ul {
      display: grid;
      gap: 8px;
      margin-top: 20px;
      padding: 0;
      list-style: none;
      color: var(--color-ink-3);
      font-size: 14px;
      line-height: 1.65;
    }
    .service-copy li {
      position: relative;
      padding-left: 16px;
    }
    .service-copy li::before {
      content: '—';
      position: absolute;
      left: 0;
      color: var(--color-accent);
    }
    .service-link {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      min-height: 44px;
      margin-top: 24px;
      color: var(--color-accent);
      font-size: 14.5px;
      font-weight: 500;
      text-decoration: underline;
      text-underline-offset: 5px;
    }
    .service-media {
      margin: 0;
      overflow: hidden;
      border: 1px solid var(--color-rule-2);
      background: var(--color-surface);
      aspect-ratio: 4 / 3;
    }
    .service-media img {
      display: block;
      width: 100%;
      height: 100%;
      object-fit: cover;
    }
    .service-media--portrait img {
      object-position: center 28%;
    }
    .case-mini-grid {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 10px;
      margin: 0;
      padding: 0;
      list-style: none;
    }
    .case-mini-grid li {
      display: grid;
      grid-template-columns: 38px minmax(0, 1fr);
      gap: 11px;
      align-items: start;
      min-height: 126px;
      padding: 14px;
      border: 1px solid var(--color-rule-2);
      background: rgb(255 255 255 / 42%);
    }
    .case-mini-icon {
      width: 38px;
      height: 38px;
      object-fit: contain;
    }
    .case-mini-grid strong,
    .case-mini-grid span {
      display: block;
    }
    .case-mini-grid strong {
      font-size: 13px;
      line-height: 1.55;
      font-weight: 600;
      color: var(--color-ink);
    }
    .case-mini-grid span {
      margin-top: 7px;
      font-size: 11.5px;
      line-height: 1.5;
      color: var(--color-accent);
    }

    /* ---------- 關於講師：人物檔案式資訊層級 ---------- */
    .instructor-profile {
      display: grid;
      grid-template-columns: minmax(220px, 0.72fr) minmax(0, 1.28fr) minmax(180px, 0.62fr);
      gap: clamp(28px, 4vw, 56px);
      align-items: start;
      padding: clamp(56px, 8vw, 96px) 0;
      border-top: 1px solid var(--color-rule);
    }
    .instructor-photo {
      margin: 0;
      overflow: hidden;
      border: 1px solid var(--color-rule-2);
      aspect-ratio: 4 / 5;
      background: var(--color-surface);
    }
    .instructor-photo img {
      display: block;
      width: 100%;
      height: 100%;
      object-fit: cover;
      object-position: center 28%;
    }
    .instructor-main h2,
    .featured-head h2 {
      font-family: var(--font-serif);
      font-size: clamp(32px, 4vw, 52px);
      line-height: 1.1;
      font-weight: 600;
      color: var(--color-ink);
    }
    .instructor-name {
      margin-top: 18px;
      font-size: 14px;
      font-weight: 600;
      color: var(--color-accent);
    }
    .instructor-intro {
      max-width: 50ch;
      margin-top: 16px;
      font-size: 15px;
      line-height: 1.9;
      color: var(--color-ink-2);
    }
    .instructor-actions {
      display: flex;
      flex-wrap: wrap;
      align-items: center;
      gap: 14px 22px;
      margin-top: 20px;
    }
    .instructor-actions .service-link {
      margin-top: 0;
    }
    .instructor-socials {
      display: flex;
      gap: 8px;
    }
    .instructor-socials a {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: 44px;
      min-height: 44px;
      padding: 0;
      border: 1px solid var(--color-rule-3);
      border-radius: 999px;
      color: var(--color-ink-2);
      text-decoration: none;
      transition:
        color 0.18s var(--ease),
        border-color 0.18s var(--ease),
        background-color 0.18s var(--ease);
    }
    .instructor-socials svg {
      width: 17px;
      height: 17px;
    }
    .instructor-facts {
      margin: 0;
      border-top: 1px solid var(--color-rule-2);
    }
    .instructor-facts div {
      padding: 14px 0 16px;
      border-bottom: 1px solid var(--color-rule);
    }
    .instructor-facts dt {
      margin-bottom: 6px;
      font-size: 10.5px;
      font-weight: 600;
      letter-spacing: 0.12em;
      color: var(--color-accent);
    }
    .instructor-facts dd {
      margin: 0;
      font-size: 12.5px;
      line-height: 1.65;
      color: var(--color-ink-2);
    }

    /* ---------- 關於講師下方：三篇精選文章 ---------- */
    .featured-posts {
      padding: clamp(56px, 8vw, 96px) 0 0;
      border-top: 1px solid var(--color-rule);
    }
    .featured-head {
      display: flex;
      align-items: end;
      justify-content: space-between;
      gap: 24px;
    }
    .featured-head > a {
      min-height: 44px;
      color: var(--color-accent);
      font-size: 13px;
      font-weight: 500;
      text-decoration: underline;
      text-underline-offset: 5px;
    }
    .featured-list {
      display: grid;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: clamp(18px, 3vw, 34px);
      margin: 28px 0 0;
      padding: 0;
      list-style: none;
    }
    .featured-list li {
      min-width: 0;
      border-top: 1px solid var(--color-rule-2);
    }
    .featured-list a {
      display: flex;
      height: 100%;
      flex-direction: column;
      padding: 18px 0 0;
      color: inherit;
      text-decoration: none;
    }
    .featured-list img {
      display: block;
      width: 100%;
      height: auto;
      aspect-ratio: 1200 / 630;
      object-fit: cover;
      border: 1px solid var(--color-rule);
    }
    .featured-meta {
      display: flex;
      justify-content: space-between;
      gap: 12px;
      margin-top: 14px;
      font-family: var(--font-mono);
      font-size: 10px;
      line-height: 1.5;
      color: var(--color-ink-3);
    }
    .featured-meta span {
      color: var(--color-accent);
    }
    .featured-list h3 {
      margin-top: 8px;
      font-size: 15px;
      line-height: 1.65;
      font-weight: 600;
      color: var(--color-ink);
      transition: color 0.18s var(--ease);
    }
    .featured-go {
      margin-top: auto;
      padding-top: 14px;
      font-size: 12px;
      color: var(--color-accent);
    }
    @media (max-width: 767px) {
      .service-details {
        padding-top: 64px;
      }
      .service-row {
        padding: 48px 0;
      }
      /* 手機版把文案容器攤平成 service-row 的 grid 子項，才能在不複製圖片的
         前提下，讓媒體落在「介紹與重點」之後、「查看連結」之前。桌機仍保留
         service-copy／media 左右雙欄，不受這組順序影響。 */
      .service-copy {
        display: contents;
      }
      .service-kicker {
        order: 1;
        margin-bottom: -18px;
      }
      .service-copy h2 {
        order: 2;
      }
      .service-copy > p:not(.service-kicker) {
        order: 3;
        margin-top: -10px;
      }
      .service-copy ul {
        order: 4;
        margin-top: -10px;
      }
      .service-media {
        order: 5;
        aspect-ratio: 16 / 10;
      }
      .service-link {
        order: 6;
        margin-top: -10px;
      }
      .case-mini-grid {
        order: 5;
      }
      .case-mini-grid li {
        grid-template-columns: 32px minmax(0, 1fr);
        min-height: 112px;
        padding: 12px;
      }
      .case-mini-icon {
        width: 32px;
        height: 32px;
      }
      .case-mini-grid strong {
        font-size: 12px;
      }
      .instructor-profile {
        grid-template-columns: minmax(0, 1fr);
        gap: 24px;
        padding: 56px 0;
      }
      .instructor-photo {
        width: min(74vw, 280px);
        aspect-ratio: 4 / 5;
      }
      .instructor-intro {
        font-size: 14px;
      }
      .instructor-facts {
        display: grid;
        grid-template-columns: repeat(3, minmax(0, 1fr));
      }
      .instructor-facts div {
        padding: 12px 10px 14px 0;
      }
      .instructor-facts div + div {
        padding-left: 10px;
        border-left: 1px solid var(--color-rule);
      }
      .instructor-facts dd {
        font-size: 11px;
      }
      .featured-posts {
        padding-top: 56px;
      }
      .featured-head {
        align-items: end;
      }
      .featured-list {
        grid-template-columns: minmax(0, 1fr);
        gap: 30px;
        margin-top: 22px;
      }
    }

    /* ---------- 結尾 CTA ---------- */
    .closer {
      padding-top: 76px;
    }
    @media (min-width: 768px) {
      .closer {
        padding-top: 120px;
      }
    }
    .closer-p {
      margin-top: 18px;
      font-size: 15.5px;
      line-height: 1.95;
      color: var(--color-ink-2);
      max-width: 52ch;
    }

    /* ============================================================
       hover 一律加裝置條件：觸控裝置上 :hover 會「黏住」，
       點過的元件會停在 hover 狀態直到點別的地方。
       :focus-visible 另外寫一份、不設任何條件。
       ============================================================ */
    @media (hover: hover) and (pointer: fine) {
      .btn-line:hover {
        background: #05b34c;
        border-color: #05b34c;
        transform: translateY(-1px);
      }
      .instructor-socials a:hover {
        color: var(--color-accent);
        border-color: var(--color-accent);
        background: rgb(255 255 255 / 60%);
      }
      .featured-list a:hover h3 {
        color: var(--color-accent);
      }
    }

    /* 鍵盤焦點：不靠 hover 也要有等價的視覺回饋（不設裝置條件） */
    .instructor-socials a:focus-visible,
    .featured-list a:focus-visible {
      outline: 2px solid var(--color-accent);
      outline-offset: 3px;
    }

    /* ============================================================
       逐段浮現：內容一律寫在 HTML 裡，只有「顯示與否」交給 CSS。
       沒有 JS 時 .js 不會加上，全部維持完整可見。
       IntersectionObserver 掛掉時另有 2 秒逾時強制顯示（見上方 script）。
       ============================================================ */
    .home.js .reveal {
      opacity: 0.3;
      transform: translateY(30px);
      transition:
        opacity 0.6s var(--ease) var(--d, 0ms),
        transform 0.6s var(--ease) var(--d, 0ms);
    }
    .home.js .reveal.is-in {
      opacity: 1;
      transform: translateY(0);
    }

    /* 首屏：0 + 70 + 140ms 延遲，各 200ms —— 總揭露 340ms */
    .home.js .hero-reveal {
      opacity: 0.3;
      transform: translateY(30px);
      transition:
        opacity 0.2s var(--ease) var(--d, 0ms),
        transform 0.2s var(--ease) var(--d, 0ms);
    }
    .home.js .hero-reveal.is-in {
      opacity: 1;
      transform: translateY(0);
    }

    @media (prefers-reduced-motion: reduce) {
      .home.js .reveal,
      .home.js .hero-reveal,
      .home.js .reveal.is-in,
      .home.js .hero-reveal.is-in {
        opacity: 1 !important;
        transform: none !important;
        transition: none !important;
      }
      /* 只關過渡，不寫任何 :hover 選擇器。
         「所有 hover 收在 @media (hover:hover) and (pointer:fine) 裡」是硬規則，
         在這裡補一條 .btn-line:hover{transform:none} 會是全站唯一一條沒有裝置
         條件的 hover 規則。而且它也沒有必要：那顆按鈕的 translateY(-1px) 本來就
         只在有滑鼠的裝置上生效，過渡一關就是瞬間位移，不構成前庭風險的動態。 */
      .btn,
      .terminal-typed,
      .terminal-command > i,
      .terminal-thinking,
      .terminal-done,
      .terminal-thinking i {
        transition: none !important;
        animation: none !important;
      }
      .terminal-typed {
        max-width: 18em;
      }
      .terminal-command > i,
      .terminal-thinking {
        display: none;
      }
      .terminal-done {
        opacity: 1;
        transform: none;
      }
    }
  </style>
</Base>
