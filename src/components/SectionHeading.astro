---
// 章節標題：lg 用於首頁大區塊，md 用於子頁面小節。
//
// 字級改成全站共用的階梯（定義與理由見 src/styles/global.css 的「內頁字級階梯」）：
//   md → .t-h2     clamp(22px, 2.6vw, 28px)
//        上限 28px 直接沿用首頁原型 B-v4 的「區塊提問」層級（.q--sec），
//        讓首頁與內頁的 h2 是同一個尺寸，不是兩套系統。
//   lg → .t-h2-lg  clamp(26px, 3.4vw, 34px)
//        首頁區塊要比內頁小節重一階，但仍遠低於展示級 H1 的 96px。
//
// 注意：.t-h2 / .t-h2-lg 是無層級 CSS，會蓋過 Tailwind 的 text-* utility。
// 呼叫端若要改字級，請改這裡的階梯，不要在 class 裡塞 text-3xl。
interface Props {
  size?: 'lg' | 'md';
  class?: string;
}

const { size = 'lg', class: className } = Astro.props;
---

<h2 class:list={['font-semibold text-ink', size === 'lg' ? 't-h2-lg' : 't-h2', className]}>
  <slot />
</h2>
